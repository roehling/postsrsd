#!/bin/sh
SRS_SECRET=${1:-@SYSCONF_DIR@/@PROJECT_NAME@.secret}
if [ ! -e "$SRS_SECRET" ] ; then
    echo "Generating secret key"
    umask 0077
    @DD@ if=/dev/urandom bs=18 count=1 | @BASE64_ENCODE@ >"$SRS_SECRET"
fi
